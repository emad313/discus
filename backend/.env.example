# Backend Environment Variables
NODE_ENV=development
PORT=3000
# Use 0.0.0.0 to listen on all interfaces (required for Docker)
HOST=0.0.0.0

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://postgres:password@localhost:5432/discus

# Redis Configuration
REDIS_URL=redis://localhost:6379

# JWT Configuration
JWT_SECRET=dev-secret-key-please-change-in-production
JWT_EXPIRE=7d

# CORS Configuration
# Allow both development (5173) and production (80) ports
CORS_ORIGIN=http://localhost:5173,http://localhost

# Mediasoup Configuration
MEDIASOUP_LISTEN_IP=0.0.0.0
MEDIASOUP_ANNOUNCED_IP=127.0.0.1
MEDIASOUP_MIN_PORT=10000
MEDIASOUP_MAX_PORT=11000
MEDIASOUP_NUM_WORKERS=4

# TURN/STUN Server Configuration
STUN_URL=stun:stun.l.google.com:19302

# Logging
LOG_LEVEL=debug
